[project]
name = "claude-server"
version = "0.1.0"
description = "MCP server for Claude Code CLI via headless mode"
requires-python = ">=3.14"
dependencies = ["mcp>=1.27.0"]

[project.scripts]
claude-server = "claude_server.server:main"
