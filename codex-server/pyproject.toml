[project]
name = "codex-server"
version = "0.1.0"
description = "MCP server for Codex CLI via app-server protocol"
requires-python = ">=3.14"
dependencies = ["mcp>=1.27.0"]

[project.scripts]
codex-server = "codex_server.server:main"
