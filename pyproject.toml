[project]
name = "ai-commander"
version = "0.2.0"
description = "Four independent MCP servers bridging agents to Gemini, Codex, Kimi, and Claude"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = ["gemini-server", "codex-server", "kimi-server", "claude-server"]

[tool.uv.sources]
gemini-server = { workspace = true }
codex-server = { workspace = true }
kimi-server = { workspace = true }
claude-server = { workspace = true }
